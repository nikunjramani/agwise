name: agtech_farmer
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.0
  flutter_secure_storage: ^3.3.3
  image_picker:
  shared_preferences: ^2.0.5
  sqflite: ^1.3.0
  path_provider:
  firebase_core: ^1.2.1
#  firebase_auth: ^1.2.0
  flutter_map: ^0.12.0
  geolocator: ^7.0.3
  flutter_map_marker_popup:
  geocoder: ^0.2.1
  mapbox_search: ^2.1.0+1
  video_player: ^2.1.1
  firebase_messaging: ^10.0.1
  fluttertoast: ^8.0.7
  permission_handler: ^8.0.1
  flutter_local_notifications: ^6.0.0
  firebase_core_platform_interface: ^4.0.1
  firebase_messaging_platform_interface: ^3.0.1
  firebase_messaging_web: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  plugin_platform_interface: ^2.0.0


flutter:

  uses-material-design: true
  assets:
    - assets/images/
    - assets/videos/